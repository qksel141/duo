services:
  - type: web
    name: duo
    runtime: node
    plan: free
    buildCommand: npm install --include=dev && npm run build && cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_BASE_URL
        value: ""
